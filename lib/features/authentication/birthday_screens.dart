import 'package:flutter/material.dart';

class BirthdayScreens extends StatelessWidget {
  const BirthdayScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
